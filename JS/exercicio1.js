/*// 1) Vamos criar uma função simples para que possa ser executada. Ao ser executada esta primeira função possa realizar o calculo - a partir de um argumento numérico - o valor de uma porcentagem que também será oferecido como segundo argumento dessa função.

function calculandoValores(a, b) {
    return (a * b) / 100

}

let resultado = calculandoValores(500, 35)
alert(`O seu resultado de 35% de 500 é: ${resultado}`);
*/

/* // 2) Criar e executar uma segunda função para obter uma conversão de valores de temperatura - medidas em graus. Converter Farenheit para Celsius e uma outra que faz a conversão de Celsius para Fahrenheit.
//Criar duas funções

function deCelsiusparaFarenheit(a){
    return (a * 9/5) + 32
}

let conversaoCF = deCelsiusparaFarenheit(20)
alert(`Olá! Seja bem-vindo a nossa conversão de valores, a sua coonversão de 20° Celsius para Feirenreit ficou: ${conversaoCF} F°`)


function deFarenheitparaCelsius(a) {
    return (a - 32) * 5/9
}

let conversaoFC = deFarenheitparaCelsius(50)
alert(`Olá! Seja bem-vindo a nossa conversão de valores, a sua coonversão de 50° Feirenreit para Celsius ficou: ${conversaoFC} C°`)

// }*/


// 3) Criar uma nova função para retornar um valor booleano - verdadeiro ou falso - para verificar e informar se um determinado número atribuído a uma variável é primo. 

/*function verificarPrimos(numero) {
    for (let i = 2; i <= numero; i++) 

    if ((numero % i == 0)) {  //verifica se tem algum resto 
        return alert('Ei cara! Esse número nem é primo. =/');
    } else {
        return alert(`Beleza! Seu número ${numero} é primo! =]`);
    }
}

let numero = verificarPrimos(97)*/

//Começo a partir do 2
// Ele tem só 2 divisores
// Como o 2 é um número primo começamos com 2
// Se o resto da divisão for igual a zero ele não é primo


